include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true

linter:
  rules:
    - prefer_final_locals
    - avoid_empty_else
    - unawaited_futures
